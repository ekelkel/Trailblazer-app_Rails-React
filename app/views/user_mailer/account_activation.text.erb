Hi <%= @user.name %>,

Welcome to Trailblazer! Click on the link below to activate your account:

<%= "#{ActionMailer::Base.default_url_options[:host]}/activate_account?validationToken=#{@user.activation_token}&email=#{@user.email}" %>